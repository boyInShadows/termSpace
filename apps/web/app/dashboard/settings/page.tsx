import { AccountSettings } from "@/features/dashboard/account-settings";

export const metadata = { title: "Settings" };

export default function DashboardSettingsPage() {
  return <AccountSettings />;
}
